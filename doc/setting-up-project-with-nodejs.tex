to create package.json, run npm init -y
package.json keeps track of the dependencies
install stuff by running npm install [package name]

make sure to gitignore the node_modules directory
make sure to commit package.json and package-lock.json

after cloning a project, make sure to run npm install . to install all the dependencies

% Preamble
\documentclass[11pt]{article}

% Packages
\usepackage{amsmath}

% Document
\begin{document}



\end{document}
